export const LOCATION_PERMISSION =
  'AES Driver requires your permissions to capture location at every 500 meters after accepting the load. Location information is used to identify the truck location during load delivery.'
export const LOCATION_API = ''

export const API = Object.freeze({
  BASE_URL: 'https://jsonplaceholder.typicode.com/',
  USERS: 'users',
  POSTS: 'posts',
})

export const ACTION = Object.freeze({
  USER_LOGIN: 'UserLogin',
  USER_LOGOUT: 'UserLogout',
})
